/**
 * Prisma seed placeholder.
 * This project currently uses file-backed local store for demo runtime.
 * When Prisma client is installed, seed ADMIN user and base stations.
 */
console.log('Run prisma db seed after installing prisma client.');
